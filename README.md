## TradingView

### Alert Message Format
```json
{
  "ticker": "{{ticker}}",
  "price": {{close}},
  "volume": "{{volume}}",
  "exchange": "{{exchange}}",
  "action": "buy",
  "interval": "{{interval}}"
}
```
